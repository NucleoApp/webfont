/*------------------------
	font icons
-------------------------*/
{% for glyph in glyphs%}
.nc-icon.{{glyph.name}}:before {
  content: "\{{ glyph.unicode[0].charCodeAt(0).toString(16) }}";
}
{% endfor %}
